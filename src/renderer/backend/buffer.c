/**
 * @file buffer.c
 * @brief GPU buffer abstraction
 *
 * Buffers are chunks of GPU memory - vertex buffers, index buffers, uniforms,
 * storage buffers, whatever. This provides a unified interface across backends.
 */

#include <bavarian3d/memory.h>
#include <bavarian3d/types.h>

/* Placeholder */
